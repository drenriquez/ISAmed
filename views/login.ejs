<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - ISAmed</title>
  <!-- Bootstrap CSS -->
  <link href="/node_modules/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa; /* Colore di sfondo grigio chiaro */
    }

    .login-form {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff; /* Colore di sfondo bianco */
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Ombra leggera */
    }

    .login-form h2 {
      margin-bottom: 20px;
      color: #343a40; /* Colore del testo scuro */
    }

    .login-form .form-control {
      border-color: #ced4da; /* Colore della bordo dei campi di input */
    }

    .login-form .btn-primary {
      background-color: #007bff; /* Colore di sfondo del pulsante primario */
      border-color: #007bff; /* Colore del bordo del pulsante primario */
    }

    .login-form .btn-primary:hover {
      background-color: #0056b3; /* Colore di sfondo del pulsante primario al passaggio del mouse */
      border-color: #0056b3; /* Colore del bordo del pulsante primario al passaggio del mouse */
    }

    /* Aggiunto stile per il logo */
    .login-logo {
      text-align: center;
      margin-bottom: 20px;
    }

    .login-logo img {
      width: 80px; /* Regola la larghezza dell'immagine */
      height: auto; /* Imposta l'altezza automaticamente per mantenere le proporzioni */
    }
  </style>
</head>
<body>

  <div class="container mt-5">
    <div class="row">
      <div class="col-md-6 m-auto">
        <div class="login-form">
          <!-- Logo -->
          <div class="login-logo">
            <img src="/images/vvf_logo.png" alt="VVF Logo">
          </div>

          <h2 class="text-center"><%= title %></h2>
          <form action="/login" method="post">
            <div class="mb-3">
              <label for="username" class="form-label">Username</label>
              <input type="text" class="form-control" id="username" name="username" required>
            </div>
            <div class="mb-3">
              <label for="password" class="form-label">Password</label>
              <input type="password" class="form-control" id="password" name="password" required>
            </div>
            <div class="text-center">
              <button type="submit" class="btn btn-secondary">Login</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap Bundle with Popper -->
  <script src="/node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
